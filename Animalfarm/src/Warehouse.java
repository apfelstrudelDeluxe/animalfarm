public class Warehouse extends Employee  {

    public Warehouse (String firstName, String secondName, int svn, Location location, String sex, int salary, Address address) {
        super(firstName, secondName, svn, location, sex, salary, address);
    }
}
